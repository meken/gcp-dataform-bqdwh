config {
    type: "table",
    schema: "dwh",
    tags: ["dimension"]
}

select
    ${keys.surrogate("credit_card_id")} as credit_card_key,
    credit_card_id,
    card_type
from ${ref("stg_credit_card")}
